IMAP_PASSWORD=your_imap_password
# Required only when [smtp] is configured in config.toml
# SMTP_PASSWORD=your_smtp_password
RUST_LOG=info
# Optional: Matrix secrets can come from the environment instead of config.toml
# MATRIX_ACCESS_TOKEN=syt_...
# MATRIX_RECOVERY_KEY=EsXX xxxx ...
